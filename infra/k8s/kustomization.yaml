apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: fastboop
configurations:
  - kustomizeconfig.yaml
resources:
  - frontdoor
  - frontdoor-edge
  - image-automation
  - gha-proxy
configMapGenerator:
  - name: live-version
    envs:
      - live-version.txt
